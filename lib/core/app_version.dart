/// Version affichée et utilisée pour la vérification de mise à jour.
/// À incrémenter en même temps que `pubspec.yaml` et `installer.iss`
/// (MyAppVersion) à chaque nouvelle release GitHub.
const String kAppVersion = '1.0.0';
